<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.MybatisTrySample.dao.BookDao">
<!--    id検索-->
    <select id="findById" resultType="com.example.MybatisTrySample.entity.Book">
        SELECT
        id,
        book_name,
        volume_num,
        author_name,
        published_date
        FROM
        booktable
        WHERE
        id = #{id}
    </select>
<!--    すべてのidを抽出-->
    <select id="findAll" resultType="com.example.MybatisTrySample.entity.Book">
        SELECT
        id,
        book_name,
        volume_num,
        author_name,
        published_date
        FROM
        booktable
    </select>

    <select id="insertBook">
        INSERT INTO booktable(
        id,
        book_name,
        volume_num,
        author_name,
        published_date
        )
        VALUES (
        #{id},
        #{bookName},
        #{volumeNum},
        #{authorName},
        #{publishedDate}
        )
    </select>
</mapper>